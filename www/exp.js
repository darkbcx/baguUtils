angular.module('expApp', ['ngMaterial', 'ngMessages', 'ngMdIcons', 'baguUtils'])
    .controller('expCtl', ['$scope', 'baguAuth0Service', 'baguCommonService', expCtl])
;

function expCtl($scope, baguAuth0Service, baguCommonService) {
    var self = this;

    // -- Properties init

    // -- Methods

    //====================================================================================================
    // -- Constructor.. kind of

}

